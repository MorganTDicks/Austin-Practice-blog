export function AuthPermissions(){
    // What about the authentication token? 
    // What stops a hack that just makes authentication return true? 
    // Receive an authentication key from the authentication server
    // anyone and anything can read the key, but only the auth server is able to generate valid keys.

    // Received the auth token, sends it through to the database to check authentication level, returns userID & authentication level.

}